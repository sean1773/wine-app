import React from "react";
import Card from "../UI/Card";

const QuestionCard = (props) => {
    console.log(props[1])
    return (
        <Card>
            <h2>Question</h2>
        </Card>
    )
};

export default QuestionCard;
